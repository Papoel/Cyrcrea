{% include 'layout/partials/html/_head' %}

<div class="container">
    <div class="card d-flex justify-content-center">
        <div class="card-header">
            <h1 class="h3 fw-bold">Email de confirmation</h1>
        </div>
        <div class="card-body">
            <img style="width: 250px" class="mx-auto d-block" src="{{ asset('images/logo/logo.svg') }}" alt="Logo CyrCrea"/>
            <div>
                <p class="lead text-center">
                    Veuillez confirmer votre adresse email en cliquant sur le lien suivant : <br><br>
                </p>
                    <a class="btn btn-primary p-3"
                       href="{{ signedUrl }}">Confirmer mon e-mail
                    </a>.
                <div class="callout callout-success w-50 mx-auto mt-5">
                    <span class="text-center d-block fst-italic text-muted">
                        Ce lien expirera dans {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}.
                    </span>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <p class="text-muted text-center my-6">
                La boutique CyrCrea. <br>
                12 rue de blabla<br>
                77555 Brie Comte Robert <br>
            </p>
        </div>
    </div>
</div>
