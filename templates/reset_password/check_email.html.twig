{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
    {{ include('partials/_title_section.html.twig', {name:"Envoi de l'e-mail de réinitialisation du mot de passe"}) }}

    <div class="container mt-5 w-50">
        <div class="card border-light rounded">
            <div class="card-header text-center">
                <h3 class="link">Réinitialisation du mot de passe</h3>
            </div>
            <div class="card-body">
                <div class="p-3">
                    <p class="lead">
                        Si un compte correspondant à votre adresse e-mail existe, alors un e-mail
                        vient d'être envoyé.
                        Vous y trouverai un lien que vous pouvez utiliser pour réinitialiser
                        votre mot de passe.
                    </p>
                    <div class="bg-dark rounded">
                        <p class="text-center text-info lead p-2">
                            Ce lien expirera {{ resetToken.getExpiresAt()|format_datetime('full','short', locale='fr') }}.
                            <span class="d-block text-danger font-weight-bold">
                                ({{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}).
                            </span>
                        </p>
                    </div>
                    <p class="lead">Si vous ne recevez pas d'e-mail, vérifiez votre dossier
                        de courrier indésirable (spam) ou votre adresse e-mail.
                    </p>
                    <div class="d-flex justify-content-center my-4">
                        <a
                            class="btn btn-md btn-outline-primary me-1"
                            href="{{ path('app_forgot_password_request') }}">
                            essayez à nouveau
                        </a>
                    </div>
                </div>
            </div>
            <div class="card-footer mb-4">
                <div class="d-flex justify-content-center mt-2">
                    <p class="fs-6">
                        Vous n'avez pas de compte ?
                        <a class="link" href="{{ path('app_register')}}"> Inscrivez-vous maintenant.</a>
                    </p>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
